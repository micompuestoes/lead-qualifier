[pytest]
# Permite importar core.tools, etc. desde la raíz de lead-qualifier
pythonpath = .
testpaths = tests
# test_agent.py es un script de humo manual (llama a la API real y necesita
# todas las dependencias instaladas); no se recoge en la suite automática.
addopts = --ignore=tests/test_agent.py

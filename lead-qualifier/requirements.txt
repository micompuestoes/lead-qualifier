# Lead Qualifier — Dependencias
# Instalar con: pip install -r requirements.txt

# ── Core ──────────────────────────────────────
anthropic>=0.40.0          # Claude API y tool use
fastapi[all]>=0.115.0      # Framework HTTP (incluye uvicorn, pydantic, etc.)
uvicorn[standard]>=0.32.0  # Servidor ASGI
pydantic[email]>=2.9.0     # Validación de modelos con soporte de email
python-dotenv>=1.0.0       # Lectura del .env en local

# ── Base de datos ──────────────────────────────
sqlalchemy>=2.0.0          # ORM/Core para SQLite (dev) y PostgreSQL (prod)
psycopg2-binary>=2.9.9     # Driver PostgreSQL — necesario en producción

# ── HTTP / SSL ────────────────────────────────
httpx>=0.27.0              # Cliente HTTP para el SDK de Anthropic
certifi>=2024.0.0          # Certificados SSL fallback
truststore>=0.9.1          # Certificados del SO — fix SSL en Windows/Microsoft Store

# ── Autenticación multi-tenant ────────────────
python-jose[cryptography]>=3.3.0  # Verificación de JWT (tokens de Clerk)

# ── IMAP email-to-lead ────────────────────────
apscheduler>=3.10.0        # Job scheduler para el polling IMAP cada 10 min
cryptography>=42.0.0       # Fernet: cifrado de contraseñas IMAP en la BD
